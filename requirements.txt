# Flask Framework and Extensions
Flask>=2.0
Flask-Login>=0.6
Flask-Limiter>=2.0
Flask-WTF>=1.2 # CSRF protection

# Database: stdlib sqlite3 (no external package needed)

# Rate-limit storage backend (used when RATELIMIT_STORAGE_URI=redis://...)
redis>=5.0

# HTTP Requests
requests>=2.25

# QR Code Generation (includes Pillow)
qrcode[pil]>=7.0 # Using the 'pil' extra installs Pillow automatically

# Utility
shortuuid>=1.0

# Production WSGI server
gunicorn>=21.0
